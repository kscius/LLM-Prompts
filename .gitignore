# Local-only drafts (not versioned)
DATOS/

# Orchestrator / scout plans (local only; do not commit)
.cursor/plans/

# Cursor MCP local overrides (if you clone ks-cursor-orchestrator, ignore secrets there)
**/mcp.config.local.json
